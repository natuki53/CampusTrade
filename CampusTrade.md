# 学内フリーマーケットシステム「CampusTrade」要件定義書

## 1. システムの目的

学内における教科書、参考書、生活用品等のリユースを促進し、学生間の交流と資源の有効活用を支援する。

---

## 2. ターゲットユーザー

- **一般学生**：商品の出品、購入、および学生間での受け渡し調整を行う。
- **管理者**：不適切な出品の監視や、カテゴリの管理を行う。

---

## 3. 機能要件（1チーム 4から5人）

### 3.1 ユーザー管理機能

- **新規登録・ログイン**：学生番号、パスワードによる認証。（Spring Security）
- **プロフィール編集**：連絡先（ニックネーム）の登録。
- **マイページ**：自分の「出品履歴」および「購入履歴」の確認。

---

### 3.2 商品管理機能

- **商品出品**
  - 項目：商品名、説明、価格（0円含む）、カテゴリ（教科書・家具・家電等）、状態（新品同様・傷あり等）、商品画像、出品者ID（表示はニックネーム）。

- **商品編集・削除**
  - 自分の出品した情報の修正。

- **商品検索・一覧**
  - キーワード検索、カテゴリ検索。
  - 「販売中」のもののみ表示するフィルタリング。

---

### 3.3 取引管理機能（設計の最重要ポイント）

- **購入申し込み**
  - 商品詳細画面からの購入意思表示。

- **ステータス管理**
  - 以下の状態遷移を管理する。

  1. **出品中（OPEN）**
  2. **交渉中 / 取引中（LOCKED）**：購入者が決まった状態
  3. **取引完了（CLOSED）**：手渡しが完了した状態

- **メッセージ機能**
  - 商品ページ上での公開Q&A、または取引成立後の非公開メッセージ（簡易的なもの）。

---

### 3.4 管理者機能（管理者ログイン時のみ利用可能）

- **商品一覧**
  - 出品中、交渉中、取引完了ごと。

- **商品削除**
  - 利用規約違反（学内禁止物など）の出品を非表示にし「禁止」と表示する。

---

## 4. 画面一覧（UI/UX設計の対象）

1. **トップ画面**：新着商品の一覧
2. **商品検索結果画面**：検索条件に合致する商品一覧
3. **商品詳細画面**：情報の閲覧、および購入ボタン
4. **商品出品・編集画面**：入力フォームとバリデーション
5. **マイページ**：出品・購入状況のサマリー
6. **ログイン・会員登録画面**

---

## 5. データモデル案（ER図のベース）

以下のエンティティ間のリレーション（OneToMany、ManyToOne）を意識させます。

- **Users（ユーザー）**
  - ユーザーID
  - パスワード
  - ニックネーム
  - 役割（Role）

- **Products（商品）**
  - 商品ID
  - 出品者ID
  - 購入者ID（null可）
  - 商品名
  - 説明
  - 価格
  - 状態
  - カテゴリID

- **Categories（カテゴリ）**
  - カテゴリID
  - カテゴリ名

- **Messages（メッセージ）**
  - ID
  - 商品ID
  - 送信者ID
  - 受信者ID
  - コメント
  - 既読区分

---

## 6. 非機能要件（教育的な制約）

技術スタッフの理解を深めるために、以下の制約を設けることを推奨します。

- **入力バリデーション**
  - 価格に負の数値が入らない。
  - 未入力項目を許容しない等。

- **セキュリティ**
  - ログインしていないユーザーは「購入」や「出品」ができない。
  - URL直打ち対策。

- **例外処理**
  - 存在しない商品IDを指定された場合に、適切なエラーページ（404等）を表示する。

---

# メッセージ機能の設計案

取引成立後、あるいは成立前のやり取りを保存するためには、「メッセージ」または「コメント」を管理する専用のテーブルが不可欠です。

特に「非公開メッセージ」として実装する場合、どの商品に関するやり取りか、誰から誰への発言かを紐づける必要があります。

5人でのグループワークとして、教育的にも非常に良い設計課題になります。具体的なテーブル構成案と、設計のポイントを整理します。

---

## 1. メッセージテーブル（Messages）の構成案

以下のようなカラム構成が一般的です。

| カラム名 | 型 | 説明 |
|---|---|---|
| id | BigInt（PK） | メッセージのユニークID |
| product_id | BigInt（FK） | 紐づく商品のID（どの取引に関する話か） |
| sender_id | BigInt（FK） | 送信者のユーザーID |
| receiver_id | BigInt（FK） | 受信者のユーザーID（※） |
| content | Text / Varchar | メッセージ本文 |
| created_at | Timestamp | 送信日時 |

※「特定の商品詳細ページに紐づく掲示板形式」にする場合は、`receiver_id` を持たせず、その商品の「出品者」と「購入希望者」が時系列で投稿を表示する形でも実装可能です。

---

## 2. ER図のイメージとリレーション

Spring Boot（Spring Data JPA）で実装する場合、以下のエンティティ間のリレーションを意識させることになります。

- **Product : Message = 1 : 多**
  - 1つの商品に対して、複数のメッセージがぶら下がる。

- **User : Message = 1 : 多**
  - 1人のユーザーは、複数のメッセージを送信する。
